# projet-documents

Structure minimale pour un service d'upload et commandes (Node.js + MySQL).

Installation:

```bash
npm install
cp .env.example .env # et modifier
npm run dev
```

(This README was left in place — remove it manually to fully match the requested structure.)
